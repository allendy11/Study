# if / for

# 조건문 if
if True:
  ...
elif:
  ...
else:
# 특이사항 없음


# 반복문 
## for 
for a in list # 리스트를 순회하기 위해서는 in 을 활용하며 이때 a 의 값은 인덱스가 아니라 값이 들어가게 된다. (JS 에서는 of 와 같음 / in 은 인덱스)
              # for 문에서는 초기식을 사용하지 않으므로 초기식이 필요한 경우 while 을 사용한다.