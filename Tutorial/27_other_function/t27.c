// getchar, putchar
// gets, puts

#include <stdio.h>

int main()
{
  // getchar, putchar
  // char ch;
  // ch = getchar();
  // putchar(ch);

  // gets, puts
  char str[100];
  gets(str);
  puts(str);
}